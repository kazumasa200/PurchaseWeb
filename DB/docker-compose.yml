version: '3'

services:
  db:
    image: postgres:14
    container_name: PurchaseWeb
    ports:
      - 5432:5432
    volumes:
      - ./script:/docker-entrypoint-initdb.d
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=postgres